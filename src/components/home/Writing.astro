---
const drafts = [
  {
    title: 'rag sistemlerinde chunking: fixed, semantic, late',
    desc: 'aynı dökümanı üç farklı yöntemle bölmek, retrieval skorunu nasıl etkiliyor?',
    reading: '~8 dk okuma',
    href: '/yazilar#taslak',
  },
  {
    title: 'agentic mimarilerde halüsinasyonu düşürmek için multi-query generation',
    desc: 'react ajanının sorgularını nasıl çeşitlendiriyoruz ve bunun son cevaba etkisi ne oluyor?',
    reading: '~10 dk okuma',
    href: '/yazilar#taslak',
  },
];
---

<section id="yazilar" class="mx-auto max-w-[1200px] px-5 mt-24">
  <div class="mb-8">
    <h2 class="font-mono text-sm text-ink-300">yazılar</h2>
    <p class="text-sm text-ink-400 mt-1">henüz az, ama başlayalım</p>
  </div>

  <div class="grid md:grid-cols-2 gap-5">
    {drafts.map((d) => (
      <a
        href={d.href}
        class="group rounded-xl border border-dashed border-ink-700 bg-ink-900/20 p-6 hover:border-accent/40 transition-all duration-200"
      >
        <div class="flex items-center gap-2 mb-3">
          <span class="text-[10px] font-mono text-ink-400 border border-ink-700 rounded px-1.5 py-0.5">
            taslak
          </span>
          <span class="text-[10px] font-mono text-ink-400">{d.reading}</span>
        </div>
        <h3 class="text-sm text-ink-100 group-hover:text-accent transition-colors duration-200 leading-snug">
          {d.title}
        </h3>
        <p class="text-xs text-ink-400 mt-2 leading-relaxed">{d.desc}</p>
      </a>
    ))}
  </div>
</section>
